# bench_flowlog disabled in Phase 2C - depends on deleted DD backend
# This benchmark will be re-enabled when columnar backend provides equivalent performance tests

datagen = executable(
  'datagen',
  files('datagen.c'),
  install: false,
)
